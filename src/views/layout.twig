<!DOCTYPE HTML>
<html>
<head>
<title>EMI RFI Shielding from Omega Shielding Products</title>
</head>
<body>
{% if notifications|length %}
<ol>
    {% for notification in notifications %}
        <li>{{ notification }}</li>
    {% endfor %}
</ol>
{% endif %}
<div class="sidebar">
    <h3>Categories</h3>
    <ol>
        {% for category in categories %}
            <li><a href="{{ path('category', {slug: category.slug}) }}">{{ category.slug }}</a></li>
        {% endfor %}
    </ol>
</div>
{% block content '' %}
</body>
</html>
